version: '3'
name: tmwu_authentication_api_dev
services:
  api:
    container_name: tmwu_authentication_api_dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file:
      - .env
    ports:
      - '6000:6000'
    volumes:
      - ./src:/app/src
    expose:
      - '6000'
