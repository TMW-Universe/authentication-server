version: '3'
name: tmwu_authentication_dev
services:
  api:
    container_name: tmwu_authentication_dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file:
      - .env
    ports:
      - '5001:5001'
    volumes:
      - ./src:/app/src
    expose:
      - '5001'
